form that you can enter a users name and per the instruction you have to know the users name and you have to type in the users name and do a .find on the db so it will find whatver user name is typed in.after it finds it it will render that to the dom. we need a friends list to render the frinds list in.
when you type in another users name it should save it